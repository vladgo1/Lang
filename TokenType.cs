namespace lang
{
    public enum TokenType
    {
        NUMBER,PLUS,MINUS,MULTILPY,DIVIDE,
        LPAR,RPAR,
        VARIABLE,TYPEOF,INT,REAL,BOOL,COMMA,
        PROGRAM, VAR, BEGIN,
        EOL,
        EOF
    }
}